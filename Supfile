# Supfile
---
version: 0.5

networks:
  mac:
    hosts:
      - localhost

commands:
  switch-shell:
    run: chsh -s /bin/zsh

  install-homebrew-packages:
    run: brew bundle

  install-npm-packages:
    run: npm install -g npm-check-updates eslint

  install-pip-packages:
    run: pip install virtualenv pythonpy

targets:
  bootstrap:
    - switch-shell
    - install-homebrew-packages
    - install-npm-packages
    - install-pip-packages
